<analysis>
<original_problem_statement>
The user's vision has evolved into creating a Digital Islamic Super App designed to be a scalable, multi-lingual, AI-powered platform for a global audience. The goal is to build one of the world's best Islamic digital platforms, serving as an ecosystem for education, guidance, worship tracking, and knowledge.

The latest set of high-level product requirements, superseding all previous ones, are:
-   **Mobile-First & Scalable Architecture**: A modern, high-performance PWA that can support millions of users.
-   **Multi-Language Support**: Initial support for Turkish, Arabic, English, Indonesian, and Urdu.
-   **User Accounts & Profiles**: Secure Google login with profiles to track worship stats, reading progress, and personal settings.
-   **Comprehensive Quran Module**: Full Quran text with search, bookmarks, progress tracking, and an advanced audio player (verse-by-verse, full surah, background play).
-   **Advanced Tafsir System**: Explanations from various scholars for each verse.
-   **Rich Hadith Module**: A database of 1000+ hadiths, categorized and searchable.
-   **Advanced Dhikr System**: Digital counter, daily goals, progress graphs, and reminders.
-   **Prayer Tools**: Accurate, location-based prayer times with notifications and a Qibla compass.
-   **AI Islamic Guide**: An AI assistant powered by a RAG architecture, using the app's Quran, Hadith, and Tafsir data to provide referenced, reliable answers to religious questions.
-   **Offline Mode**: Access to core content without an internet connection.

From this, the user has requested an immediate **Phase 1** implementation focusing on:
1.  Making the app work flawlessly on mobile (interpreting Expo Go as a PWA).
2.  Ensuring Google login is robust.
3.  Establishing a proper database system.
4.  Enhancing the Ask a Scholar feature.
5.  Expanding the Islamic Knowledge Cards and the Quiz question bank.
6.  Making the AI chatbot fully operational and responsive.
7.  Ensuring the PWA is installable (interpreting APK as PWA installation).
</original_problem_statement>

**User's preferred language**: Turkish

**what currently exists?**
-   **Frontend**: A feature-rich React PWA with a dark-green/gold theme. It includes a dashboard, Quran reader with AI kıssa generation, a notes system, an Ask a Scholar page, settings, and a highly interactive, Trivia Crack-style Quiz page with animations and a leaderboard. The app is PWA-enabled with a service worker, install prompt, and a global error boundary. Voice commands are also implemented.
-   **Backend**: A monolithic FastAPI application () serving all data from JSON files. It provides endpoints for authentication (Google & Guest), Quran, Hadith, knowledge cards, AI kıssa generation, notes, a full quiz system (categories, questions, leaderboard), an AI chatbot, and a  endpoint for monitoring.

**Last working item**:
-   **Last item agent was working**: The agent was working on the new Super App requirements (Phase 1). It began by diagnosing and fixing a critical bug in the AI Chatbot, where an incorrect LLM provider was being used (usage: openai [-h] [-v] [-b API_BASE] [-k API_KEY] [-p PROXY [PROXY ...]]
              [-o ORGANIZATION] [-t {openai,azure}]
              [--api-version API_VERSION] [--azure-endpoint AZURE_ENDPOINT]
              [--azure-ad-token AZURE_AD_TOKEN] [-V]
              {api,tools,migrate,grit} ...

positional arguments:
  {api,tools,migrate,grit}
    api                 Direct API calls
    tools               Client side tools for convenience

options:
  -h, --help            show this help message and exit
  -v, --verbose         Set verbosity.
  -b API_BASE, --api-base API_BASE
                        What API base url to use.
  -k API_KEY, --api-key API_KEY
                        What API key to use.
  -p PROXY [PROXY ...], --proxy PROXY [PROXY ...]
                        What proxy to use.
  -o ORGANIZATION, --organization ORGANIZATION
                        Which organization to run as (will use your default
                        organization if not specified)
  -t {openai,azure}, --api-type {openai,azure}
                        The backend API to call, must be `openai` or `azure`
  --api-version API_VERSION
                        The Azure API version, e.g.
                        'https://learn.microsoft.com/en-us/azure/ai-
                        services/openai/reference#rest-api-versioning'
  --azure-endpoint AZURE_ENDPOINT
                        The Azure endpoint, e.g.
                        'https://endpoint.openai.azure.com'
  --azure-ad-token AZURE_AD_TOKEN
                        A token from Azure Active Directory,
                        https://www.microsoft.com/en-
                        us/security/business/identity-access/microsoft-entra-
                        id
  -V, --version         show program's version number and exit for a  model), causing timeouts. After fixing this and verifying with a  check, the agent started implementing new features in parallel: creating a  for the Qibla compass and adding 200 more questions to the quiz database.
-   **Status**: IN PROGRESS
-   **Agent Testing Done**: Y (Backend AI chat and health endpoints were tested with . Frontend implementation is partial.)
-   **Which testing method agent to use?**: both. The new features (Qibla, expanded quiz, scholar chat) require backend testing via  and full end-to-end UI validation with the frontend testing agent.
-   **User Testing Done**: N

**All Pending/In progress Issue list**:
-   **Issue 1: Critical - Backend is Not Scalable (Priority: P0)**
    -   The entire backend is a single monolithic  file and uses JSON files as a database. This directly contradicts the user's new requirement for a scalable app that can handle millions of users. The agent has previously identified this as needing a refactor but postponed it. It is now a critical architectural flaw.
    -   **Attempted fixes**: None.
    -   **Next debug checklist**:
        1.  Propose a refactoring plan to the user to break  into logical modules (e.g., , , , ).
        2.  Propose a database migration from JSON files to a scalable solution like PostgreSQL or MongoDB Atlas.
        3.  Begin the refactoring process once approved.
    -   **Why fix this issue and what will be achieved with the fix?**: To build a stable, maintainable, and scalable foundation that can support the user's Super App vision. Without this, the app will face performance and reliability issues.
    -   **Status**: NOT STARTED
    -   **Is recurring issue?**: Y
    -   **Should Test frontend/backend/both after fix?**: Both (Extensive regression testing will be required).
    -   **Blocked on other issue**: None, but requires user approval.

-   **Issue 2: Broken Text-to-Speech (TTS) Feature (Priority: P1)**
    -   The TTS feature is failing due to the Emergent LLM Key budget being exceeded. The underlying code dependency () was fixed in a previous session, but the budget issue was never communicated to the user.
    -   **Attempted fixes**: Dependency was installed. No fix for the budget issue.
    -   **Next debug checklist**:
        1.  Use the  tool to inform the user that their LLM budget for the TTS feature is exhausted.
        2.  Guide them to their Emergent profile to add more balance to the Universal Key.
    -   **Why fix this issue and what will be achieved with the fix?**: This will restore the Listen functionality for Ayahs and Hadiths, a key feature of the app.
    -   **Status**: NOT STARTED
    -   **Is recurring issue?**: Y (It's a carry-over from the last fork).
    -   **Should Test frontend/backend/both after fix?**: Backend, then Frontend.
    -   **Blocked on other issue**: Blocked on user action (adding funds).

**In progress Task List**:
-   **Task 1: Complete Phase 1 of Super App Development (Priority: P0)**
    -   This task involves implementing the user's immediate requests from message #273 to move towards the Super App vision.
    -   **Where to resume**:
        1.  **Integrate New Quiz Content**: Update  to load and serve the newly added 200 quiz questions from .
        2.  **Implement Qibla Compass**: The  file has been created. Add the route in , add a navigation link (e.g., in Settings or on the Dashboard), and implement the compass logic using device sensors.
        3.  **Fix Ask a Scholar Endpoint**: The frontend is calling , but the backend endpoint is . Update the API call in  to use the correct URL.
        4.  **Expand Knowledge Cards**: Add more entries to .
        5.  **Ensure PWA/Mobile Experience**: Verify the PWA is fully installable and the UI is responsive and fluid on mobile screen sizes. This addresses the user's Expo Go / APK request.
    -   **What will be achieved with this?**: This will complete the user's most immediate feature requests and demonstrate significant progress towards the new Super App vision.
    -   **Status**: IN PROGRESS
    -   **Should Test frontend/backend/both after fix?**: Both
    -   **Blocked on something**: None.

**Upcoming and Future Tasks**
-   **Upcoming Tasks (Super App - Phase 2)**:
    -   **(P1) Backend Refactor & DB Migration**: Address the critical scalability issue by modularizing  and migrating from JSON files to a real database.
    -   **(P1) Multi-Language Implementation**: Add a language selector and i18n infrastructure to support TR, AR, EN, ID, UR.
    -   **(P2) Advanced Quran Audio Player**: Implement background playback and a mini-player UI.
    -   **(P2) Hadith & Tafsir Modules**: Create backend endpoints and frontend pages to serve and display a large Hadith collection and multi-scholar Tafsirs.
-   **Future Tasks (Super App - Phase 3)**:
    -   **(P2) AI Guide with RAG**: Implement the advanced Retrieval Augmented Generation system for the AI assistant.
    -   **(P3) Advanced User Profiles & Stats**: Build out the user profile to track worship activities with graphs and stats.
    -   **(P3) Full Offline Mode**: Cache core content (Quran, Hadiths) for offline access.
    -   **(P3) World Prayer Times Map**: A visual feature to see prayer times globally.

**Completed work in this session**
-   **AI Chatbot Fixed**: Resolved a critical bug causing timeouts by correcting the LLM provider configuration in .
-   **Full-Featured Quiz System**: Built a complete, highly-styled quiz module with 300+ questions, categories, scoring, a timer, answer feedback, and a functional leaderboard.
-   **Quran Enhancements**: Successfully implemented Quran search, AI-powered kıssa generation for verses, and a Notes system for saving ayahs and kıssas.
-   **PWA & Stability Implemented**: Converted the app into a Progressive Web App (PWA) with a service worker, manifest file, and an Add to Home Screen prompt. Added a global  to prevent the entire app from crashing on component errors.
-   **Critical Bug Fixes**:
    -   Resolved a guest login failure by correcting the backend URL in the frontend.
    -   Fixed a bug in the Notes API where user authentication was failing on direct function calls.
-   **Voice Commands Verified**: Confirmed the existing Web Speech API implementation for voice commands is functional and added a notes command.

**Earlier issues found/mentioned but not fixed**
-   **Issue 1: Exceeded LLM budget for TTS**
    -   **Debug checklist**: Inform the user they need to add funds to their Emergent LLM Key.
    -   **Why to solve this issue and what will be achieved with this?**: The TTS feature is completely blocked until the user takes action.
    -   **Should Test frontend/backend/both after fix**: Backend
    -   **Is recurring issue?**: Y

**Known issue recurrence from previous fork**
-   **Issue recurrence in previous fork**: User providing massive, architecture-altering requirements. This has just happened with the Super App request.
-   **Recurrence count**: High.
-   **Status**: BLOCKED (on user behavior). Mitigation is to break down the large vision into phased, manageable, and verifiable tasks, and secure user approval for each phase before implementation.

**Code Architecture**


**Key Technical Concepts**
-   **Frontend**: React, PWA (Service Workers, Manifest), React Router, Axios, Tailwind CSS, Framer Motion, Web Speech API.
-   **Backend**: FastAPI (Python), Pydantic, Pymongo.
-   **AI/LLM**:  library for calling OpenAI and Anthropic models (used for Kıssa, Chatbot, TTS).
-   **Database**: JSON files (This is a major scalability bottleneck).

**key DB schema**
-   No formal DB. Data is in JSON files.
-    (implied): 
-   : 

**changes in tech stack**
-   The application has been converted into a Progressive Web App (PWA).

**All files of reference**
-   : The monolithic heart of the application. All business logic, AI calls, and data serving happens here. It's the primary file for backend changes and the top candidate for refactoring.
-   : A new, large, and complex component that implements the entire quiz game UI and logic.
-    & : Core files for the new PWA functionality.
-   : A new but incomplete file for the Qibla compass feature.
-   : New file containing 200 additional quiz questions.
-   : Needs a fix for the API endpoint URL.

**Areas that need refactoring**:
-   ****: This is the most critical area. The file is over 3000 lines long and handles everything. It MUST be broken down into smaller, domain-specific routers (e.g., , , ) to make the application scalable and maintainable, as per the user's new request.
-   **Data Storage**: The use of JSON files as a database is unsustainable for the user's vision. A migration to a proper database (e.g., PostgreSQL) is necessary.

**key api endpoints**
-   : (GET) New health check endpoint.
-   : (POST) The AI chatbot endpoint.
-   : (GET) Lists all quiz categories.
-   : (POST) Starts a quiz for a given category.
-   : (POST) Submits quiz results and updates the leaderboard.
-   : (GET) Retrieves the quiz leaderboard.
-   : (POST) The correct endpoint for the Ask a Scholar feature.

**Critical Info for New Agent**
-   **New Mandate**: Your primary goal is to execute Phase 1 of the Super App plan (user message #273). This is the new source of truth.
-   **Address User Misconceptions**: The user uses terms like Expo Go and APK. This is a React Web App. Interpret these requests as ensure a first-class, installable mobile PWA experience. Do not attempt to convert the project to React Native.
-   **Prioritize Scalability Refactor**: While implementing Phase 1 features, you MUST also address the scalability problem. The monolithic  and JSON database are direct blockers to the user's vision. You should propose and begin refactoring the backend as the next high-priority task after completing the current feature set.
-   **Broken TTS Feature**: The TTS feature is still unusable due to an LLM budget issue. You must inform the user so they can take action.

**documents created in this job**
-    (Continuously updated with the latest user requirements).

**Last 10 User Messages and any pending HUMAN messages**
1.  **#284 (Agent)**: Acknowledged the AI service is slow and promised to fix all issues systematically. **(IN PROGRESS)**
2.  **#273 (User)**: Provided a concrete list for Phase 1 of the Super App: make it Expo Go ready, fix Google auth, set up a database, enhance scholar/knowledge/chat features, and make it APK-ready. **(IN PROGRESS)**
3.  **#271 (User)**: Clarified the Super App should be developed in phases, starting with Phase 1, and be cost-effective. **(Acknowledged)**
4.  **#268 (User)**: Sent a massive, detailed new product requirement for a global, scalable Islamic Super App. This replaced all previous plans. **(Acknowledged, new PRD)**
5.  **#266 (Agent)**: Finished implementing PWA features and error boundaries, showed a screenshot of the final login page, and was ready to finish. **(COMPLETED)**
6.  **#226 (User)**: Requested the app be made ready for Expo Go and that Google login be re-activated, with a focus on stability and cost efficiency. **(Superseded by #268)**
7.  **#224 (Agent)**: Confirmed that the testing agent passed all flows for the Quiz, Search, and Notes features and was ready to finish. **(COMPLETED)**
8.  **#184 (Agent)**: Showed the working quiz answer feedback and stated readiness to move to the next features (audio player, push notifications, voice commands). **(COMPLETED)**
9.  **#116 (Agent)**: Acknowledged the new Quiz redesign task and promised to be efficient. **(COMPLETED)**
10. **#102 (User)**: After the agent finished the last task, the user provided a new, large set of feature requests: push notifications, voice commands, server refactoring, and a complete redesign of the quiz system to be like Trivia Crack with a massive question bank and leaderboard. **(COMPLETED)**

**Project Health Check:**
-   **Broken**:
    -   Text-to-Speech (TTS): Non-functional due to an exhausted Emergent LLM Key budget. User needs to be notified.
-   **Mocked**:
    -   The Qibla Compass page () exists but is not implemented or wired into the app.

**3rd Party Integrations**
-   **Anthropic Claude**: Uses Emergent LLM Key (for AI Chatbot).
-   **OpenAI GPT**: Uses Emergent LLM Key (for AI Kıssa generation).
-   **OpenAI TTS**: Uses Emergent LLM Key (for text-to-speech, **CURRENTLY BROKEN**).
-   **Emergent-managed Google Auth**: Used for user login.

**Testing status**
-   **Testing agent used after significant changes**: YES. The agent used the testing agent to validate the backend quiz endpoints and the full frontend quiz flow.
-   **Troubleshoot agent used after agent stuck in loop**: NO
-   **Test files created**: None.
-   **Known regressions**: None.

**Credentials to test flow:**
-   **Google Login**: Use the standard Google OAuth flow.
-   **Guest Login**: Click the Misafir Olarak Devam Et button.

**What agent forgot to execute**
-   The agent identified the need to refactor  but postponed it. Given the new user requirements for scalability, this is no longer optional.
-   The agent never informed the user about the exhausted LLM budget for the TTS feature, leaving it broken across multiple forks.
</analysis>
